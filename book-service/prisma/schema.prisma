generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Book {
  id          String   @id @default(uuid())
  title       String
  authorId    String
  publishedYear Int
  createdAt   DateTime @default(now())
}